
export interface TimelineEvent {
  year: string;
  title: string;
  subtitle: string;
  description: string;
  imagePlaceholder?: boolean;
  imageUrl?: string;
  link?: boolean;
  titleColor?: string;
}